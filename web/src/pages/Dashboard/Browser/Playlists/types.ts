export type BrowserPlaylist = {
  is_synced: boolean;
  is_analyzed: boolean;
  description: string;
  owner_id: string;
  version: string;
  image_url: string;
  public: boolean;
  shared: boolean;
  id: string;
  name: string;
  spotify_id: string;
};
